import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webantek.prims',
  appName: 'Prims',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
